# 晨会录音与本地转录

工作 Tab 内并列「晨会记录／工作待办／工作灵感」。本版不改变原有音量键手势。

## 先验证实际收音

1. Android Studio 连接手机运行项目，打开秒记 → 工作 → 晨会记录。
2. 开启秒记无障碍服务、选择原来的同步文件夹；首次点击录音时允许麦克风权限。
3. 点「60 秒试录」，回到企业微信的测试会议，开启手机外放。
4. 自己说一句，请另一位参会人说一句；确认对方仍能正常听见你，会议声音没有变化。
5. 60 秒后自动结束，或点浮动按钮／通知「结束录音」。回到晨会记录，结束会议后回放。
6. 必须听清双方声音才继续正式使用。车载蓝牙另做一次相同测试；它与手机外放的音频路由、回声消除不同。

试录只保存音频，不调用任何云端语音模型。录音会使用温和的动态增益抬升较远的人声；音量指示只证明麦克风有信号，不证明双方声音都录进去了。
检测到系统静音会明确提示并保留记录。无系统静音也可能存在回声消除造成的远端发言缺失，需回放确认。
耳机对方声音不能靠普通麦克风方案保证录入。

## 正式使用

点「开始晨会录音」后回到企业微信；录音期间有系统通知和可点击结束的浮动标签。
结束后手机只保存并同步 `录音.wav` 和 `记录.json`，状态为 `pending_local`；不会把晨会音频发送给阿里百炼或其他云端语音模型。
Mac mini 从同步目录读取待处理录音，用本地模型转录后写回 `转录.md`，并把 `记录.json` 更新为 `ready`。手机再次进入晨会记录页时会接收同一条记录的更新结果。

完整录音按 16 kHz 单声道 PCM16 WAV 保存（约每分钟 1.92 MB），最多单次 3 小时。
文字由 Mini 本地流程生成，不在手机端调用阿里百炼。手机端不提供“上传转录”按钮，只在同步失败时提供“重新同步”。

## 文件与恢复

手机首次同步目录：`晨会记录/<唯一编号>/录音.wav`、`记录.json`；Mini 处理完成后增加 `转录.md` 并更新 `记录.json`。
手机先用本地文件暂存，成功后导出到你选择的同步目录。同步失败会保留本机副本并提供「重新同步」。
进程中断后重新进入页面会修复已写入音频的文件头；不会悄悄重新开启麦克风或自动重新上传。
若本机尚未完整导出，不要卸载 App 或清除 App 数据。已有财务／健康文件不受影响。

## 验证范围

代码编译及本地测试不能证明 Pixel 与企业微信的双路收音成功；以手机外放和车载蓝牙各自的回放结果为准。
Mini 端到端验证需要等 Mini 在线并实际处理一条 `pending_local` 记录；手机端不读取 Key、不上传现有录音到语音模型。

参考：
- https://developer.android.com/media/platform/sharing-audio-input
- https://developer.android.com/develop/background-work/services/fgs/service-types
- https://help.aliyun.com/zh/model-studio/qwen-asr-api-reference
