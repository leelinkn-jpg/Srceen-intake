# 成长记录文件接口

## 目录结构

每次开始努力时，App 创建一个 UUID 作为记录 ID。结束后在同步根目录写入：

```text
成长记录/
  <记录ID>/
    记录.json
```

外部 AI、Mac mini 或用户可以把任意学习文件放进同一目录：

```text
成长记录/
  <记录ID>/
    记录.json
    投资学习笔记.md
    AI学习评价.md
    阅读材料.pdf
```

除 `记录.json` 和隐藏文件外，其余文件都作为“学习文件”显示在 App 中。Markdown、TXT、JSON 可以直接预览；PDF 等文件调用手机上的对应应用打开。

## 记录格式

`记录.json` 示例：

```json
{
  "id": "6e28455b-55ae-4f45-a534-7e1f767d54f8",
  "activityId": "价值投资",
  "activityName": "价值投资",
  "startedAt": 1789647000000,
  "durationMs": 2700000
}
```

关联依据只有 `id`。日期、项目名称和时长用于展示，不用于猜测文件归属。

## 两种接收方式

1. 自动：把文件同步到 `成长记录/<记录ID>/`，重新进入成长页后自动显示。
2. 手动：打开某次努力的详情，点击“导入文件”，所选文件会复制到对应记录目录。

App 不区分笔记、学习痕迹和 AI 评价，也不绑定某一个 AI 软件。
