# 财务输入白名单

根目录：`财务`

- `账本.csv`：日期、类型、分类、金额、备注。
- `卡片.csv`：卡名、类型、当前余额。
- `转账记录.csv`：账户间转移，仅影响账户余额结构。
- `交易记录.csv`：证券买卖流水。
- `持仓.csv`：当前证券持仓与成本。

共享输入：`综合报告/反馈/*.json`中属于`finance`的反馈。

禁止：其他领域原始数据、临时文件、模型自行联网补行情。需要行情时必须由用户另行授权数据源。
