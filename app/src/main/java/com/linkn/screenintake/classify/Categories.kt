package com.linkn.screenintake.classify

/**
 * 分类的固定枚举列表——支出这份沿用鲨鱼记账里原本的设置，收入这份是新加的（原来的记账
 * 只认支出，没有收入这个概念）。这两份列表同时喂给模型（system prompt 里要求只能从这
 * 里面选）和用来给分类配小图标（见 ui/CategoryIcons.kt），改列表只用改这一个地方。
 */
object Categories {
    val EXPENSE = listOf(
        "充电", "餐饮", "过路费", "因公差旅", "停车费", "应酬社交", "通讯", "小甜蜜",
        "汽车", "日用消费", "运动", "娱乐", "住房", "医疗", "保险", "数码", "旅行",
        "学习", "亲情", "装修", "其他"
    )

    val INCOME = listOf(
        "工资", "奖金", "兼职收入", "理财收益", "报销", "红包礼金", "打麻将", "其他收入"
    )
}
